# GitHub Pages user guide

The `userguide/` directory is a dependency-free static site. Enable **Settings → Pages → GitHub Actions**; `.github/workflows/pages.yml` publishes this directory. It also opens locally from the desktop application's **User Guide** button.
